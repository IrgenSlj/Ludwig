# Ludwig daemon dependencies (kept separate from the dependency-light core).
# Install into a venv:  python3 -m venv .venv && ./.venv/bin/pip install -r daemon/requirements.txt
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27        # also used by fastapi.testclient
pytest>=8.0        # daemon tests
Pillow>=10.0       # used by ludwig.py (is_void render check)
